version: "3.7"

services:
  web:
    build:
      context: .
    ports:
      - "$PORT:$PORT"
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py runserver 0.0.0.0:$PORT"
    depends_on:
      - db
  db:
    image: postgres
